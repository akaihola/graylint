# Constraints for pip to pin dependencies to the oldest supported versions.
# This is used in a GitHub Workflow matrix job which ensures everything
# still works against the oldest supported versions of both the Python
# interpreter and Python ependencies. Keep this up-to-date with minimum
# versions in `pyproject.toml`.
darkgraylib==0.0.1
toml==0.10.0
types-toml==0.10.4
